# Operating-system and editor metadata
.DS_Store
Thumbs.db
.vscode/

# Local Adobe SDK copies (download separately; do not commit vendor SDKs)
/SDK/

# macOS builds and Xcode state
/native/build/*
/native/control-surface/build/
/native/windows/build/
*.app/
!/native/build/listener-build.txt
!/native/build/PR FX Shortcut Listener.app/
!/native/build/PR FX Shortcut Listener.app/**
*.dSYM/
*.xcarchive/
DerivedData/
*.xcuserstate
xcuserdata/

# Windows builds and Visual Studio state
/native/windows/bin/
/native/windows/obj/
*.dll
*.exe
*.pdb
*.deps.json
*.runtimeconfig.json
*.suo
*.user
*.VC.db
*.VC.opendb

# Local runtime state, logs, and signing material
*.log
*.p12
*.pem
*.key
*.cer
*.mobileprovision

# Diagnostic output. The findings belong in CLAUDE.md, which IS tracked — these
# dumps are machine- and version-specific and must never be committed.
PR FX QE API.txt
ax-probe
ax-diagnostic
settings.local.json
/native/com.prfx.shortcut-listener.plist
/native/.DS_Store
/native/control-surface/.DS_Store

# Local reference tool; PR FX never imports or ships it.
/untitled folder/
